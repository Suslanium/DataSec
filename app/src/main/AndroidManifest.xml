<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.suslanium.encryptor">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.NoActionBar">
        <activity android:name=".YandexDiskSignIn"></activity>
        <activity android:name=".GoogleDriveUploadSelector" />
        <activity android:name=".GoogleDriveManager" />
        <activity android:name=".GoogleDrive" />
        <activity android:name=".oneDriveActivity" />
        <activity android:name=".passwordChange" />
        <activity android:name=".passwordAdd" />
        <activity
            android:name=".Explorer"
            android:label="@string/title_activity_explorer"
            android:theme="@style/Theme.MaterialComponents.NoActionBar" />
        <activity android:name=".MainActivity" />
        <activity android:name=".PasswordActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".EncryptorService" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>